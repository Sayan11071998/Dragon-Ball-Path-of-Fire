using UnityEngine;

namespace DragonBall.Enemy
{
    public class BuuEnemyModel : BaseEnemyModel
    {
        // Add Buu-specific properties here

        public BuuEnemyModel(float _maxHealth, float _attackDamage, float _attackCooldown)
            : base(_maxHealth, _attackDamage, _attackCooldown)
        {
            // Initialize Buu-specific properties
        }

        // Override base methods if needed or add Buu-specific methods
    }
}